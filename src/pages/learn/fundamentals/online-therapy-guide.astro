---
import BaseLayout from '../layouts/BaseLayout.astro';
import { ArrowRight, CheckCircle, AlertCircle, Laptop, Video, MessageSquare, Phone, DollarSign, Shield, Clock } from 'lucide-react';
---

<BaseLayout>
<div class="bg-gray-50 min-h-screen py-12 px-4 sm:px-6 lg:px-8">
      <div class="container mx-auto px-4 sm:px-6 lg:px-8">
        {/* Hero Section */}
        <div class="bg-white rounded-lg shadow-md overflow-hidden mb-8">
          <div class="md:flex">
            <div class="md:w-1/3">
              <img 
                src="/images/blog/choosing-right-therapist.jpg" 
                alt="Online Therapy Guide" 
                class="w-full min-w-0 h-full object-cover"
              />
            </div>
            <div class="p-8 md:w-2/3">
              <h1 class="text-3xl font-bold text-gray-800 mb-4">Online Therapy Guide</h1>
              <p class="text-xl text-gray-600 mb-6">
                Everything you need to know about online therapy, from how it works to what to expect in your first session and how to get the most from your experience.
              </p>
              <div class="flex flex-wrap gap-4">
                <Button 
                  as={Link} 
                  to="/tools/matchers/therapy-platform-matcher" 
                  variant="primary"
                  icon={Laptop}
                >
                  Find Your Online Therapy Match
                </Button>
                <Button 
                  as={Link}
                  to="/best/platforms/best-online-therapy-platforms" 
                  variant="outline"
                  icon={ArrowRight}
                >
                  View Top Platforms
                </Button>
              </div>
            </div>
          </div>
        </div>

        {/* What is Online Therapy */}
        <div class="bg-white rounded-lg shadow-md p-8 mb-8">
          <h2 class="text-2xl font-bold text-gray-800 mb-6">What is Online Therapy?</h2>
          <div class="md:flex gap-8">
            <div class="md:w-2/3">
              <p class="text-gray-600 mb-4">
                Online therapy (also known as teletherapy, virtual therapy, or e-therapy) is the delivery of mental health counseling and support through digital platforms. It connects clients with licensed therapists through secure video calls, phone sessions, or text-based communication.
              </p>
              <p class="text-gray-600 mb-4">
                This approach to therapy has grown significantly in recent years, offering a convenient and accessible alternative to traditional in-person therapy. Online therapy platforms typically provide subscription-based services that include regular sessions with a licensed therapist, along with additional features like messaging, resources, and tools.
              </p>
              <p class="text-gray-600">
                Research has shown that online therapy can be as effective as in-person therapy for many common mental health concerns, including anxiety, depression, and stress management. The key is finding the right platform and therapist that matches your specific needs and preferences.
              </p>
            </div>
            <div class="md:w-1/3 mt-6 md:mt-0">
              <div class="bg-indigo-50 rounded-lg p-6">
                <h3 class="font-bold text-gray-800 mb-3">Key Benefits</h3>
                <ul class="space-y-2">
                  <li class="flex items-start">
                    <CheckCircle class="h-5 w-5 text-green-500 mr-2 mt-0.5" />
                    <span>Convenient access from anywhere</span>
                  </li>
                  <li class="flex items-start">
                    <CheckCircle class="h-5 w-5 text-green-500 mr-2 mt-0.5" />
                    <span>Eliminates travel time and costs</span>
                  </li>
                  <li class="flex items-start">
                    <CheckCircle class="h-5 w-5 text-green-500 mr-2 mt-0.5" />
                    <span>Often more affordable than in-person</span>
                  </li>
                  <li class="flex items-start">
                    <CheckCircle class="h-5 w-5 text-green-500 mr-2 mt-0.5" />
                    <span>Flexible communication options</span>
                  </li>
                  <li class="flex items-start">
                    <CheckCircle class="h-5 w-5 text-green-500 mr-2 mt-0.5" />
                    <span>Greater privacy and comfort</span>
                  </li>
                </ul>
              </div>
            </div>
          </div>
        </div>

        {/* Communication Methods */}
        <h2 class="text-2xl font-bold text-gray-800 mb-6">Communication Methods</h2>
        
        {communicationMethods.map((method) => (
          <div key={method.name} class="bg-white rounded-lg shadow-md p-6 mb-6">
            <div class="flex items-start mb-4">
              <div class="bg-indigo-100 rounded-full w-12 h-12 flex items-center justify-center mr-4">
                <method.icon class="h-6 w-6 text-indigo-600" />
              </div>
              <div>
                <h3 class="text-xl font-bold text-gray-800 mb-2">{method.name}</h3>
                <p class="text-gray-600">{method.description}</p>
              </div>
            </div>
            
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <h4 class="font-bold text-gray-800 mb-2">Benefits:</h4>
                <ul class="space-y-1">
                  {method.benefits.map((benefit, i) => (
                    <li key={i} class="flex items-start">
                      <CheckCircle class="h-5 w-5 text-green-500 mr-2 mt-0.5 flex-shrink-0" />
                      <span class="text-gray-600">{benefit}</span>
                    </li>
                  ))}
                </ul>
              </div>
              
              <div>
                <h4 class="font-bold text-gray-800 mb-2">Considerations:</h4>
                <ul class="space-y-1">
                  {method.considerations.map((consideration, i) => (
                    <li key={i} class="flex items-start">
                      <AlertCircle class="h-5 w-5 text-yellow-500 mr-2 mt-0.5 flex-shrink-0" />
                      <span class="text-gray-600">{consideration}</span>
                    </li>
                  ))}
                </ul>
              </div>
            </div>
          </div>
        ))}

        {/* Choosing the Right Platform */}
        <div class="bg-white rounded-lg shadow-md p-8 mb-8 mt-12">
          <h2 class="text-2xl font-bold text-gray-800 mb-6">Choosing the Right Platform</h2>
          <p class="text-gray-600 mb-6">
            With numerous online therapy platforms available, it's important to find one that best meets your specific needs and preferences. Here are key features to consider when evaluating different platforms:
          </p>
          
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            {platformFeatures.map((feature) => (
              <div key={feature.name} class="flex items-start">
                <CheckCircle class="h-5 w-5 text-green-500 mr-2 mt-0.5 flex-shrink-0" />
                <div>
                  <h3 class="font-bold text-gray-800 mb-1">{feature.name}</h3>
                  <p class="text-gray-600">{feature.description}</p>
                </div>
              </div>
            ))}
          </div>
          
          <div class="mt-8">
            <Button 
              as={Link} 
              to="/tools/matchers/therapy-platform-matcher" 
              variant="primary"
            >
              Find Your Platform Match
            </Button>
          </div>
        </div>

        {/* CTA Section */}
        <div class="bg-gradient-to-r from-indigo-600 to-purple-600 rounded-lg p-8 text-white text-center">
          <h2 class="text-2xl font-bold mb-4">Ready to Find the Right Online Therapist?</h2>
          <p class="text-lg mb-6 max-w-2xl mx-auto">
            Now that you understand how online therapy works, take the next step in your mental health journey.
          </p>
          <div class="flex flex-wrap justify-center gap-4">
            <Button 
              as={Link} 
              to="/tools/matchers/therapy-platform-matcher" 
              variant="light"
              size="lg"
            >
              Find Your Therapy Match
            </Button>
            <Button 
              as={Link} 
              to="/best/platforms/best-online-therapy-platforms" 
              variant="outline"
              size="lg"
              class="border-white text-white hover:bg-white hover:bg-opacity-10"
            >
              Explore Top Platforms
            </Button>
          </div>
        </div>
      </div>
    </div>
</BaseLayout>
