---
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { platforms } from '../../../siteData';
import { Star, CheckCircle, XCircle, ArrowRight } from 'lucide-react';
import { platforms } from '../../../siteData';

// const platforms = []; // Now using real data from siteData

const renderStars = (rating: number) => {
  const fullStars = Math.floor(rating);
  const starsArray = Array(5).fill(0).map((_, i) => i < fullStars);
  return starsArray;
};

---

<BaseLayout>
<div class="bg-gray-50 min-h-screen">
      
      <div class="sb-container py-12">
        {/* Hero Section */}
        <div class="text-center mb-12">
          <h1 class="text-4xl font-bold text-gray-800 mb-4">Therapist Matching Systems Compared</h1>
          <p class="text-xl text-gray-600 max-w-3xl mx-auto mb-8">
            How different platforms connect you with the right therapist and which systems work best.
          </p>
        </div>

        {/* Introduction Section */}
        <div class="bg-white rounded-lg shadow-md p-8 mb-12">
          <h2 class="text-2xl font-bold text-gray-800 mb-4">Why Therapist Matching Matters</h2>
          <p class="text-gray-600 mb-4">
            Finding the right therapist is crucial for successful therapy outcomes. Research shows that the therapeutic alliance—the relationship between client and therapist—accounts for approximately 30% of positive therapy outcomes, making it one of the strongest predictors of success.
          </p>
          <p class="text-gray-600 mb-4">
            Online therapy platforms use different approaches to match clients with therapists, from sophisticated algorithms to human consultations. The effectiveness of these systems can significantly impact your therapy experience.
          </p>
        </div>

        {/* Types of Matching Systems */}
        <div class="bg-white rounded-lg shadow-md p-8 mb-12">
          <h2 class="text-2xl font-bold text-gray-800 mb-6">Types of Therapist Matching Systems</h2>
          
          <div class="mb-6">
            <h3 class="text-xl font-bold text-gray-800 mb-2">Algorithm-Based Matching</h3>
            <p class="text-gray-600 mb-2">
              Uses questionnaires and AI to match clients based on preferences, needs, and therapeutic approaches.
            </p>
            <div class="flex items-center mt-2">
              <CheckCircle class="h-5 w-5 text-green-500 mr-2" />
              <span>Quick and efficient</span>
            </div>
            <div class="flex items-center mt-1">
              <XCircle class="h-5 w-5 text-red-500 mr-2" />
              <span>May miss nuanced personal preferences</span>
            </div>
          </div>

          <div class="mb-6">
            <h3 class="text-xl font-bold text-gray-800 mb-2">Consultation-Based Matching</h3>
            <p class="text-gray-600 mb-2">
              Involves an initial consultation with a matching specialist who recommends therapists.
            </p>
            <div class="flex items-center mt-2">
              <CheckCircle class="h-5 w-5 text-green-500 mr-2" />
              <span>Human element improves matching quality</span>
            </div>
            <div class="flex items-center mt-1">
              <XCircle class="h-5 w-5 text-red-500 mr-2" />
              <span>Typically takes longer than algorithmic matching</span>
            </div>
          </div>

          <div class="mb-6">
            <h3 class="text-xl font-bold text-gray-800 mb-2">Specialty-Focused Matching</h3>
            <p class="text-gray-600 mb-2">
              Matches clients with therapists who specialize in specific conditions or therapeutic approaches.
            </p>
            <div class="flex items-center mt-2">
              <CheckCircle class="h-5 w-5 text-green-500 mr-2" />
              <span>Highly effective for specific conditions</span>
            </div>
            <div class="flex items-center mt-1">
              <XCircle class="h-5 w-5 text-red-500 mr-2" />
              <span>Limited therapist pool</span>
            </div>
          </div>
        </div>

        {/* Platform Comparison */}
        <div class="bg-white rounded-lg shadow-md p-8 mb-12">
          <h2 class="text-2xl font-bold text-gray-800 mb-6">Platform Matching Systems Compared</h2>
          
          {platforms.map((platform, index) => (
            <div key={platform.slug} class="mb-8 p-6 border rounded-lg">
              <h3 class="text-xl font-bold">{platform.name}</h3>
              <div class="flex items-center mb-2">
                {renderStars(platform.rating)}
                <span class="ml-2">{platform.rating}/5</span>
              </div>
              <p class="font-semibold">Matching System: {platform.matchingSystem}</p>
              <p class="mb-4">{platform.description}</p>
              
              <div class="mb-4">
                <h4 class="font-bold text-green-600">Pros:</h4>
                <ul class="list-disc pl-5">
                  {platform.pros.map((pro, i) => (
                    <li key={i}>{pro}</li>
                  ))}
                </ul>
              </div>
              
              <div class="mb-4">
                <h4 class="font-bold text-red-600">Cons:</h4>
                <ul class="list-disc pl-5">
                  {platform.cons.map((con, i) => (
                    <li key={i}>{con}</li>
                  ))}
                </ul>
              </div>
              
              <div>
                <h4 class="font-bold">Effectiveness Rating:</h4>
                <p>{platform.effectiveness}</p>
              </div>
            </div>
          ))}
        </div>

        {/* Tips Section */}
        <div class="bg-white rounded-lg shadow-md p-8 mb-12">
          <h2 class="text-2xl font-bold text-gray-800 mb-4">Tips for Getting the Best Match</h2>
          
          <div class="mb-4">
            <h3 class="text-lg font-semibold">1. Be specific about your needs</h3>
            <p class="text-gray-600">
              The more detailed you are about your concerns, preferences, and goals, the better the matching system can work for you.
            </p>
          </div>
          
          <div class="mb-4">
            <h3 class="text-lg font-semibold">2. Don't hesitate to switch</h3>
            <p class="text-gray-600">
              If your first match doesn't feel right, request a new therapist. Most platforms make this process simple.
            </p>
          </div>
          
          <div class="mb-4">
            <h3 class="text-lg font-semibold">3. Consider specialized platforms for specific conditions</h3>
            <p class="text-gray-600">
              If you have a specific condition like OCD or eating disorders, consider platforms that specialize in those areas.
            </p>
          </div>
          
          <div>
            <h3 class="text-lg font-semibold">4. Give it time</h3>
            <p class="text-gray-600">
              The therapeutic relationship takes time to develop. Consider giving your therapist 2-3 sessions before deciding if it's a good match.
            </p>
          </div>
        </div>

        {/* CTA Section */}
        <div class="bg-indigo-600 text-white p-8 rounded-lg text-center">
          <h2 class="text-2xl font-bold mb-4">Find Your Perfect Therapist Match</h2>
          <a href="/tools/matchers/therapy-platform-matcher" class="inline-flex items-center bg-indigo-600 text-white px-6 py-3 whitespace-nowrap rounded-md hover:bg-indigo-700 transition">Take the Matching Assessment</a>
        </div>
      </div>
    </div>
</BaseLayout>
